import colors from './colors'
import fonts from './fonts'
import elevations from './elevations'
import spacing from './spacing'

console.log({ colors, fonts, elevations, spacing })

export default {
  colors,
  fonts,
  elevations,
  spacing,
}
